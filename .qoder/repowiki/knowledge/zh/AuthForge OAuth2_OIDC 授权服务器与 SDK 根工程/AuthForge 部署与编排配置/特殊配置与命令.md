`docker/Dockerfile` 通过 `--mount=type=cache,target=/root/.conan2` 复用 Conan 缓存加速重构建；后端构建直接调用 `bash scripts/backend/build.sh Release` 以保持与 CI 一致；Helm 安装需先执行迁移 Job（`migration-job.yaml` 调用 `authforge-server --migrate-only`）。