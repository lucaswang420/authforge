首次使用前需确保存在仓库根 `paths.env`（由 `paths_env.bat`/`paths-env.ps1` 生成或手动维护），否则所有脚本直接退出；Linux/macOS 可通过 `./build.sh --install-deps` 自动安装 git/build-essential/cmake 等系统编译工具，但 C/C++ 库一律走 Conan；Sanitizer 构建需 GCC/Clang 且默认强制 Debug 模式；Windows 对应入口为 `build.bat`、`full_test.bat`、`test.bat` 等同名文件。