C++17 静态库集合；Domain 层使用 jsoncpp 与 OpenSSL（JWT/JWK/TOTP HMAC-SHA1）；Adapter 层基于 Drogon（ORM/HTTP/RedisClient/CacheMap）并可选集成 libcurl（SMTP）；通过 CMake 的 `find_package(... CONFIG REQUIRED)` 发现依赖，使用 `authforge_package()` 生成可被 find_package() 消费的 SDK 配置。