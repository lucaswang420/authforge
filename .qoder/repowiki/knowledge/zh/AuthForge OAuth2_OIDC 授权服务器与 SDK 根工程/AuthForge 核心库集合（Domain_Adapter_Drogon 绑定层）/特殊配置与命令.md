各子包自带自包含 bootstrap：若作为 `add_subdirectory` 引入，会先 `list(APPEND CMAKE_MODULE_PATH .../../../cmake)` 再 `include(Compatibility|Warnings|AuthForgePackage)`，因此 libs/* 目录本身可脱离根工程单独构建；可选功能通过顶层 `WITH_WEBAUTHN`/`WITH_SOCIAL` 选项开关，并在 CMake 中同时从源列表移除对应 .cc 并定义 `WITH_*` 编译宏。