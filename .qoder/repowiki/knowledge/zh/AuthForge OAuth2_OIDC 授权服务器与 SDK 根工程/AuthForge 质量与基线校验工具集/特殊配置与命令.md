各工具以 `python tools/<tool>/<script>.py --root <repo-root>` 形式运行，默认 `--root` 自动推导为脚本所在目录的上两级；更新基线的统一入口为 `--update-baseline`（如 `api_diff.py --update-baseline`、`migration_check.py --update-baseline`、`diff-endpoint-baseline.py --update-baseline`）；`refactor-baseline/capture.{sh,ps1}` 分别用于 Unix/Windows 环境捕获 Playwright 与 CTest 输出。