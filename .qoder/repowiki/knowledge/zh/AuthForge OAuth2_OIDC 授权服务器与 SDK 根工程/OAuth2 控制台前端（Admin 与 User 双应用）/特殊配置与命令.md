两个应用需分别进入子目录执行：`npm run dev`（admin 端口 5174，user 端口 5173）启动开发服务器；`npm run build` 构建产物；`npm run test:e2e` 运行 Playwright 端到端测试；`npm run test:unit` 运行 Vitest 单元测试。admin 需要后端在 `http://127.0.0.1:5555` 提供 `/oauth2`、`/api`、`/.well-known` 等代理目标。