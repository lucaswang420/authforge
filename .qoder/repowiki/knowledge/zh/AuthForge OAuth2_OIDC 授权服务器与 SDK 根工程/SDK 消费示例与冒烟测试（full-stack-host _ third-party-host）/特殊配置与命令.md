full-stack-host 通过顶层 CTest 的 `SdkSmoke.FullStack` 用 `ctest --build-and-test` 构建并运行，要求 `CMAKE_PREFIX_PATH` 指向构建树中的 `authforge-cmake` 包根；其 `POST_BUILD` 步骤将 `config/smoke.json` 复制到可执行目录并重命名为 `config.json`。third-party-host 按 README 说明通过 `cmake --preset windows-msvc` 构建目标 `third-party-host-smoke`，并以进程退出码 0/1 报告结果。