name: macOS CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

env:
  BUILD_TYPE: Release
  DROGON_VERSION: "v1.9.13"

jobs:
  build-and-test:
    runs-on: macos-14
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Install Drogon dependencies
        run: |
          # Refresh Homebrew formula index (do NOT upgrade preinstalled
          # packages — a blanket `brew upgrade` breaks the job whenever any
          # preinstalled formula (e.g. xcodes) lacks an arm64 bottle).
          brew update

          # Install core dependencies (no databases needed for build-only)
          brew install git cmake
          brew install jsoncpp
          brew install ossp-uuid
          brew install zlib
          brew install openssl@3 libpq hiredis
          brew install curl

      # --- Drogon build cache ---
      # Temporarily disable cache to force rebuild after key change
      # - name: Cache Drogon build
      #   id: cache-drogon
      #   uses: actions/cache@v4
      #   with:
      #     path: ${{github.workspace}}/drogon
      #     key: drogon-${{ runner.os }}-${{ env.DROGON_VERSION }}-v6-cpp17-pure-${{ env.BUILD_TYPE }}

      - name: Build Drogon
        run: |
          git clone --depth 1 --branch ${{ env.DROGON_VERSION }} https://github.com/drogonframework/drogon
          cd drogon
          git submodule update --init --recursive
          mkdir build && cd build
          export PostgreSQL_ROOT="$(brew --prefix libpq)"
          cmake .. \
            -G "Unix Makefiles" \
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} \
            -DCMAKE_CXX_STANDARD=17 \
            -DCMAKE_CXX_STANDARD_REQUIRED=ON \
            -DCMAKE_CXX_EXTENSIONS=OFF \
            -DCMAKE_INSTALL_PREFIX=/usr/local \
            -DOPENSSL_ROOT_DIR="$(brew --prefix openssl@3)" \
            -DCMAKE_FIND_FRAMEWORK=LAST \
            -DBUILD_EXAMPLES=OFF \
            -DBUILD_MYSQL=OFF \
            -DBUILD_POSTGRESQL=ON \
            -DBUILD_REDIS=ON \
            -DBUILD_TESTING=OFF
          cmake --build . --config ${{ env.BUILD_TYPE }} --parallel $(sysctl -n hw.ncpu)
          sudo cmake --install .

      - name: Configure Project CMake
        run: |
          # Use CI config which uses memory storage instead of Postgres/Redis
          cp OAuth2Server/config.ci.json OAuth2Server/config.json

          export PostgreSQL_ROOT="$(brew --prefix libpq)"
          cmake -S . -B build -G "Unix Makefiles" \
            -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} \
            -DCMAKE_CXX_STANDARD=17 \
            -DCMAKE_CXX_STANDARD_REQUIRED=ON \
            -DCMAKE_CXX_EXTENSIONS=OFF \
            -DBUILD_TESTS=ON \
            -DOAUTH2_MEMORY_TESTS_ONLY=ON \
            -DOPENSSL_ROOT_DIR="$(brew --prefix openssl@3)" \
            -DCURL_ROOT="$(brew --prefix curl)" \
            -DCMAKE_FIND_FRAMEWORK=LAST
      - name: Build
        run: cmake --build build --config ${{env.BUILD_TYPE}}

      - name: Run Tests
        working-directory: ${{github.workspace}}/build
        run: ctest -V -C ${{env.BUILD_TYPE}} --output-on-failure --timeout 120
