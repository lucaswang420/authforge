name: Windows CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

env:
  BUILD_TYPE: Release
  DROGON_VERSION: "v1.9.12"

jobs:
  build-and-test:
    runs-on: windows-2022
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Install Conan
        run: |
          pip install conan
          conan profile detect --force

      # --- Conan dependency cache ---
      # Disabled Conan cache to avoid cache-related conflicts
      # - name: Cache Conan packages
      #   uses: actions/cache@v4
      #   with:
      #     path: ~/.conan2
      #     key: conan-${{ runner.os }}-v3-cpp17-${{ hashFiles('OAuth2Backend/conanfile.txt') }}
      #     restore-keys: |
      #       conan-${{ runner.os }}-

      - name: Configure CMake using Conan
        working-directory: ${{github.workspace}}/OAuth2Backend
        run: |
          # Remove old build directory to avoid cache conflicts
          Remove-Item -Path build -Recurse -Force -ErrorAction SilentlyContinue
          conan install . --output-folder=build --build=openssl --build=missing -s build_type=${{env.BUILD_TYPE}} -s compiler.cppstd=17

      # --- Drogon build cache ---
      # Disabled Drogon cache to avoid cache key conflicts and force fresh builds
      # - name: Cache Drogon build
      #   id: cache-drogon
      #   uses: actions/cache@v4
      #   with:
      #     path: ${{github.workspace}}/drogon
      #     key: drogon-${{ runner.os }}-${{ env.DROGON_VERSION }}-v6-full-components-${{ env.BUILD_TYPE }}

      - name: Skip database installation
        run: Write-Host "Skipping PostgreSQL and Redis installation to speed up CI"

      - name: Build Drogon
        # if: steps.cache-drogon.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 --branch ${{ env.DROGON_VERSION }} https://github.com/drogonframework/drogon
          cd drogon
          git submodule update --init --recursive
          mkdir build && cd build
          cmake .. `
            -G "Visual Studio 17 2022" `
            -A x64 `
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} `
            -DCMAKE_CXX_STANDARD=17 `
            -DCMAKE_INSTALL_PREFIX="C:\Drogon" `
            -DCMAKE_TOOLCHAIN_FILE="${{github.workspace}}/OAuth2Backend/build/conan_toolchain.cmake" `
            -DCMAKE_POLICY_DEFAULT_CMP0091=NEW `
            -DBUILD_CTL=ON `
            -DBUILD_ORM=ON `
            -DBUILD_EXAMPLES=OFF `
            -DBUILD_POSTGRESQL=ON `
            -DBUILD_MYSQL=OFF `
            -DBUILD_SQLITE=ON `
            -DBUILD_REDIS=ON `
            -DBUILD_BROTLI=ON `
            -DBUILD_YAML_CONFIG=ON
          cmake --build . --config ${{ env.BUILD_TYPE }} --target install --parallel $env:NUMBER_OF_PROCESSORS

      - name: Skip database initialization
        run: |
          Write-Host "Skipping database initialization - databases not installed"
          # Add Drogon to PATH for drogon_ctl
          echo "C:\Drogon\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Prepare CI Config
        working-directory: ${{github.workspace}}/OAuth2Backend
        run: |
          # Backup original config and use CI config with memory storage
          # This must happen BEFORE cmake configure so configure_file picks it up
          mv config.json config.json.backup
          cp config.ci.json config.json
        shell: bash

      - name: Configure Project CMake
        working-directory: ${{github.workspace}}/OAuth2Backend
        run: |
          cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DCMAKE_CXX_STANDARD=17 -DCMAKE_TOOLCHAIN_FILE="${{github.workspace}}/OAuth2Backend/build/conan_toolchain.cmake" -DCMAKE_POLICY_DEFAULT_CMP0091=NEW -DCMAKE_PREFIX_PATH="C:\Drogon" -DDrogon_DIR="C:\Drogon\lib\cmake\Drogon" -DBUILD_TESTS=ON

      - name: Build
        working-directory: ${{github.workspace}}/OAuth2Backend
        run: cmake --build build --config ${{env.BUILD_TYPE}}

      - name: Run Tests
        working-directory: ${{github.workspace}}/OAuth2Backend/build
        env:
          # Ensure no accidental DB/Redis connections are attempted
          OAUTH2_DB_HOST: ""
          OAUTH2_REDIS_HOST: ""
        run: |
          # Explicitly copy CI config to test working directory as a safeguard.
          # Using relative paths to avoid backslash escaping issues in bash on Windows.
          cp ../config.ci.json ./test/${{env.BUILD_TYPE}}/config.json
          ctest -V -C ${{env.BUILD_TYPE}} --output-on-failure --timeout 180
        shell: bash

      - name: Run Performance Benchmarks
        working-directory: ${{github.workspace}}/OAuth2Backend/build/test/${{env.BUILD_TYPE}}
        env:
          OAUTH2_DB_HOST: ""
          OAUTH2_REDIS_HOST: ""
        run: |
          echo "=== Running Performance Benchmarks ==="
          ./OAuth2Test_test.exe -r Performance_OAuth2Flow
          ./OAuth2Test_test.exe -r Performance_StorageThroughput
          ./OAuth2Test_test.exe -r Performance_MemoryUsage
          ./OAuth2Test_test.exe -r Performance_LatencyPercentiles
        shell: bash

      - name: Run E2E Tests
        working-directory: ${{github.workspace}}/OAuth2Backend/build/test/${{env.BUILD_TYPE}}
        env:
          OAUTH2_DB_HOST: ""
          OAUTH2_REDIS_HOST: ""
        run: |
          echo "=== Running E2E Tests ==="
          ./OAuth2Test_test.exe -r OAuth2AuthorizationCodeFlow
          ./OAuth2Test_test.exe -r SessionManagement
          ./OAuth2Test_test.exe -r ClientAuthentication
          ./OAuth2Test_test.exe -r RedirectURIValidation
        shell: bash

      - name: Run Integration Tests
        working-directory: ${{github.workspace}}/OAuth2Backend/build/test/${{env.BUILD_TYPE}}
        env:
          OAUTH2_DB_HOST: ""
          OAUTH2_REDIS_HOST: ""
        run: |
          echo "=== Running Integration Tests ==="
          ./OAuth2Test_test.exe -r RedirectUriValidation_MemoryStorage
          ./OAuth2Test_test.exe -r RedirectUriValidation_Atomicity
          ./OAuth2Test_test.exe -r RedirectUriValidation_EdgeCases
          ./OAuth2Test_test.exe -r RedirectUriValidation_SecurityScenarios
        shell: bash

      - name: Restore original config
        if: always()
        working-directory: ${{github.workspace}}/OAuth2Backend
        run: |
          # Restore original config after tests
          mv config.json.backup config.json
        shell: bash

      # --- Upload logs on failure for debugging ---
      - name: Upload test logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-test-logs
          path: |
            ${{github.workspace}}/OAuth2Backend/build/Testing/
            ${{github.workspace}}/OAuth2Backend/logs/
          retention-days: 7
